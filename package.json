{
  "name": "strobo.fm",
  "private": true,
  "scripts": {
    "start": "npm run css && hugo server",
    "css": "postcss --dir public/css static/css/*.css",
    "test": "stylelint static/css/*.css"
  },
  "devDependencies": {
    "autoprefixer": "^9.6.4",
    "cssnano": "^4.1.10",
    "postcss-cli": "^6.1.3",
    "stylelint": "^11.0.0",
    "stylelint-config-standard": "^19.0.0",
    "stylelint-order": "^3.1.1"
  }
}
